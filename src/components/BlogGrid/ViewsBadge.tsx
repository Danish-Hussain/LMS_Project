"use client"
// ViewsBadge has been replaced with an inline badge in `BlogGrid` and `PostViews`.
// Keep a minimal stub here to avoid breakage in older caches or imports.
export default function ViewsBadge(): null {
  return null
}
